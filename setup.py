#!/usr/bin/env python3
"""
Setup script for MNN-Torch package.

This script is provided for compatibility with older build tools,
but the package is configured to use pyproject.toml with hatchling.
"""

from setuptools import setup

# This setup.py is minimal and mainly for compatibility
# The actual package configuration is in pyproject.toml
if __name__ == "__main__":
    setup()
